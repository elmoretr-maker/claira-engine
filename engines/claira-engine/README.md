# Claira Engine (canonical sources)

This directory is a **logical anchor** for Phase 10.5 layout: **Claira Engine** source files live at the **repository root** (`index.js`, `interfaces/`, `core/`, `server/` except orchestration notes, `ui/`, etc.).

Do **not** place Asset Orchestration Engine controller code here.
