/**
 * Global application mode: simulation uses mock integrations; real expects live APIs only.
 * Allowed values: "simulation" | "real"
 */

/** @type {"simulation" | "real"} */
export const SYSTEM_MODE = "simulation";
