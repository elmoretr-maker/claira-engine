node_modules/
.env
.env.*
!.env.example
# Only .env.example should be committed. Never commit real environment files.

# Vite production build (regenerate: npm run ui:build)
ui/dist/
build/
coverage/
.cache/

/output/
temp/
_clip_test.png
dev/_clip_smoke.png

eng.traineddata
claira-pdf-*
*.log
data/session_report.json
config/active_pack.json
config/pack_reference.json
packs/industryAutogen/cache/

# Isolated workspace (simulation/live per account × industry)
workspace/

# Staged tax PDF comparison uploads (ephemeral; ignore if cleanup fails)
.claira_tax_compare/

# Runtime / user-local progress tracking (source under tracking/*.js stays tracked)
tracking/images/
tracking/snapshots/
tracking/entities/

# Phase 7+ workflow runtime data (implementation will use this path)
workflow/trainer/data/

# Phase 13 — persisted feedback store (source under workflow/feedback/*.js stays tracked)
workflow/feedback/data/

# User reference images (learning) — never commit
references/user/

# Local test drops / validation fixtures (not part of source tree)
New_Arrival/
_phase*/

# Optional dev/test images
dev/test-assets/
dev/fixtures/
test_images/

Assets/
*.tmp.png
*.debug.png

.DS_Store
Thumbs.db

# Pre-generated voice lines (manifest stays tracked; run npm run generate:voice-assets)
ui/public/assets/audio/voice/*.mp3

# Integration job store (runtime data — restored on restart, never commit)
server/jobsStore.json
server/jobsStore.tmp.json
